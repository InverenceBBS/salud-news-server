"""
A function to format the resulting news articles. As yet this is just a placeholder in case we need it.

# Arguments
- `articles::Dict`: Dictionary from the `query_newsapi` function from `IOM_newsAPI_query`.
"""
function format_result(articles)
    return articles
end